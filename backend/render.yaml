services:
  - type: web
    name: qa-deck-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npx playwright install chromium
    startCommand: node server.js
    envVars:
      - key: WEBSITE_ORIGIN
        value: https://qadeck.com
    plan: free
